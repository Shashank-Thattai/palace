/*
 * The Palace — runtime.
 *
 * On Day 0 the Palace has no behaviors. The mortal enters, reads three
 * sentences, and stands in the center. Every day, the winning god may
 * add one behavior here — a flicker, a reveal, an echo, a whisper, a
 * clock that runs backward, a door that opens only when no one is
 * watching.
 *
 * Gods should not remove the things above them. They may add their own
 * behaviors after the marker below.
 */

"use strict";

document.addEventListener("DOMContentLoaded", () => {
  // GODS' BEHAVIORS GO HERE
});
